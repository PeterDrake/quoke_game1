/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SanitizeHandsFlash : MonoBehaviour
{
    public Text buttonText;

    private void Awake()
    {
        //buttonText.text = "Sanitize hands";
        StartCoroutine(BlinkText());
    }

    public IEnumerator BlinkText()
    {
        //WonOrNot = GetComponent<MiniGameMaster>().Win;
     /**   if (WonOrNot == true)
        {
            buttonText.text = Debug;
        }
        else
        {
            buttonText.text = Debug2;
        }*/
     /*   while (true)//(WonOrNot == true)
        {
            buttonText.text = " ";
            yield return new WaitForSeconds(.5f);
            buttonText.text = "Sanitize hands";
            yield return new WaitForSeconds(.5f);
        }
    }
}*/
