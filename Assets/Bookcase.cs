using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bookcase : MonoBehaviour
{
    private int state = 0;
    /*
     * for function called by InteractWithObject
     * on first pass, check if player has tools, if so, display "Press 'e' to secure bookcase"
     * if not, display text "This bookcase could fall over in an earthquake, I should secure it"
     * on second pass "MESSAGE 2"
     * on third pass "Press 'e' to stabilize the bookcase". * On exit, trigger earthquake *
     */
    
    
    
}
